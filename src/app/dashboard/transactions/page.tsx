"use client";
import { Card, CardHeader } from "@/components/ui/card";
import { StatusBadge } from "@/components/ui/status-badge";
import { formatDateTime } from "@/lib/utils";
import { useDemoStore } from "@/store/demo-store";
export default function TransactionsPage() { const { selectedBusinessId, operations, customers, programs } = useDemoStore(); const list = operations.filter((op) => op.businessId === selectedBusinessId); return <div className="space-y-6"><h1 className="text-2xl font-semibold">Операции</h1><Card><CardHeader title="История операций" description="Фильтры периода, типа, программы, сотрудника и филиала представлены demo-select’ами." /><div className="grid gap-3 p-4 sm:grid-cols-5">{["Период","Тип","Программа","Сотрудник","Филиал"].map((item) => <select key={item} className="h-10 rounded-lg border border-slate-200 px-3 text-sm"><option>{item}: все</option></select>)}</div><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Дата</th><th className="p-4">Клиент</th><th className="p-4">Программа</th><th className="p-4">Операция</th><th className="p-4">Работник</th><th className="p-4">Филиал</th><th className="p-4">Статус</th></tr></thead><tbody>{list.map((op) => <tr key={op.id} className="border-t border-slate-100"><td className="p-4">{formatDateTime(op.date)}</td><td className="p-4">{customers.find((c) => c.id === op.customerId)?.name}</td><td className="p-4">{programs.find((p) => p.id === op.programId)?.name}</td><td className="p-4">{op.change}</td><td className="p-4">{op.worker}</td><td className="p-4">{op.branch}</td><td className="p-4"><StatusBadge status={op.status} /></td></tr>)}</tbody></table></div></Card></div>; }
